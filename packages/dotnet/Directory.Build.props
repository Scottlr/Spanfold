<Project>

  <PropertyGroup>
    <LangVersion>14.0</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <VersionPrefix>0.1.0</VersionPrefix>
    <VersionSuffix Condition="'$(CI)' != 'true'">preview</VersionSuffix>
  </PropertyGroup>

</Project>
